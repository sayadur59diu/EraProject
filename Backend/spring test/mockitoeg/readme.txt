1. Directly run tests from the `src/test` directory.  
2. There are two test files:  
   a. `UserControllerPositiveTest.java` – Positive test cases (all will pass).  
   b. `UserControllerNegativeTest.java` – Negative test cases (some will fail and show errors).  
