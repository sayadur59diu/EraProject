1. Directly run tests from the `src/test` directory.  
2. There are two test files:  
   a. `CalculatorServicePositiveTest.java` – Positive test cases (all will pass).  
   b. `CalculatorServiceNegativeTest.java` – Negative test cases (some will fail and show errors).  
